export const config = {
  // Maximum file size: 10MB
  maxFileSize: 10 * 1024 * 1024,
  
  // Allowed file extensions
  allowedExtensions: ['.pdf'],
  
  // Cache duration in seconds (1 hour)
  cacheDuration: 3600,
  
  // Default filename if none provided
  defaultFilename: 'inspection.pdf',
};